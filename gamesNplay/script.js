var xpos;
var speed;

function setup(){
	createCanvas(windowWidth,windowHeight);
	background(200);

	xpos=0;
	speed=5;
}

function draw(){
	fill(35,52,120);
	stroke(0);
	ellipse(width/2,height/2,100,100);
}
